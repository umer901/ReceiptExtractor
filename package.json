{
  "name": "receipt-reimbursement-validator",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @receipt/api\" \"npm run dev -w @receipt/web\"",
    "build": "npm run build -w @receipt/shared && npm run build -w @receipt/api && npm run build -w @receipt/web",
    "test": "npm run test -w @receipt/shared && npm run test -w @receipt/api && npm run test -w @receipt/web",
    "db:migrate": "npm run db:migrate -w @receipt/api",
    "typecheck": "npm run typecheck -ws --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": { "node": ">=20" }
}
